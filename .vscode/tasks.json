{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Desplega a producció (commit + push)",
      "type": "shell",
      "command": "git add . && git commit -m 'Actualització automàtica' && git pull --rebase && git push",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
